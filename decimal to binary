#include<stdio.h>
int find(int decimal){
  if(decimal==0){
    return 0;
  }
  else
    return(decimal%2+10*find(decimal/2));
}
int main(){
  int decimal=9;
  printf("%d",find(decimal));
  return 0;
}
